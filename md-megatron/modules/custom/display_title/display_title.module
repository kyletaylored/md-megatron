<?php

function display_title_theme($existing, $type, $theme, $path) {
  return array('display_title_template' =>                  // this is the name of the template
           array(
            'variables' => array(
                'title' => null
            ),
        )
    );
}